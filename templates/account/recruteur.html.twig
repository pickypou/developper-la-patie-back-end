{% extends 'base.html.twig' %}

{% block title %}trt-mon compte{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{asset('assets/css/account.css')}}">

    {%if app.user.recruteur%}
        <h2>Bienvenue {{app.user.recruteur.username}} sur votre espace personnel</h2>
    {%else%}
        <h2>Bienvenue sur votre espace personnel</h2>
    {%endif%}

    {%if app.user.recruteur%}
     -deposer une annonce
    {%else%}
        -<a href="{{path('app_profil_recruteur')}}">compléter mon profil</a><br/>
        -deposer une annonce<br/>
    {%endif%}

    <hr/>
    <a href="{{path('app_logout')}}" class="btn btn-sm">Me deconnecter</a>
{% endblock %}